#include <stdio.h>
#include <stdarg.h>
/**
 * print_percent - print the % format
 * @args: variadic list of function
 * Return: 1 if success
 */
int print_percent(va_list args)
{
	putchar('%');

	return (1);
}
